# Architecture Documentation

## C4 Model

### Level 1: System Context

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           External Systems                               │
│                                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌────────────┐  │
│  │Trading Engine│  │Margin Engine │  │Market Data   │  │Compliance  │  │
│  │(TXNX events) │  │(RISK events) │  │Feed (MKTX)   │  │System(REGX)│  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └─────┬──────┘  │
└─────────┼─────────────────┼─────────────────┼────────────────┼──────────┘
          │                 │                 │                │
          └─────────────────┴────────┬────────┘                │
                                     │                          │
                         ┌───────────▼──────────────────────────────────────┐
                         │    Financial Notification Platform                │
                         │    POST /api/v1/events                           │
                         │    (FastAPI + Kafka + RabbitMQ + Redis)          │
                         └───────────┬──────────────────────────────────────┘
                                     │ Delivers to
               ┌─────────────────────┼─────────────────────┐
               │                     │                      │
      ┌────────▼───────┐   ┌────────▼───────┐   ┌─────────▼──────┐
      │  SMS (Twilio/  │   │  Email         │   │  Push (FCM)    │
      │  MSG91)        │   │  (SendGrid)    │   │  WhatsApp      │
      └────────────────┘   └────────────────┘   └────────────────┘
```

### Level 2: Container Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     Financial Notification Platform                          │
│                                                                              │
│  ┌─────────────────┐    ┌──────────────────┐    ┌───────────────────────┐   │
│  │   FastAPI App   │    │  Kafka Consumer  │    │  RabbitMQ Consumer    │   │
│  │   (Gunicorn +   │───▶│  (Event Fan-out) │    │  (Priority Delivery)  │   │
│  │   Uvicorn)      │    └────────┬─────────┘    └──────────┬────────────┘   │
│  │   Port: 8000    │             │                          │                │
│  └────────┬────────┘             │                          │                │
│           │                      ▼                          ▼                │
│           │             ┌────────────────┐        ┌─────────────────┐       │
│           │             │ NotificationFac│        │ Celery Workers  │       │
│           │             │ tory + Dispatch│        │ (send_notif_    │       │
│           │             │ er + Compliance│        │  task)          │       │
│           │             └────────┬───────┘        └────────┬────────┘       │
│           │                      │                          │                │
│  ┌────────▼────────────┐         │    ┌─────────────────────▼────────────┐   │
│  │   PostgreSQL 16     │◀────────┘    │   Channel Registry               │   │
│  │   (Primary store)   │              │   SMS│Email│Push│WhatsApp│InApp   │   │
│  └─────────────────────┘              └──────────────────────────────────┘   │
│                                                                              │
│  ┌──────────────────┐    ┌─────────────────┐    ┌────────────────────────┐   │
│  │   Redis 7        │    │   Apache Kafka  │    │   RabbitMQ 3.12        │   │
│  │   - Freq caps    │    │   - Events bus  │    │   - Priority queues    │   │
│  │   - DND cache    │    │   - DLQ topic   │    │   - CRITICAL/HIGH/     │   │
│  │   - Rate limit   │    │   - Analytics   │    │     MEDIUM/LOW         │   │
│  └──────────────────┘    └─────────────────┘    └────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Level 3: Component Diagram (Notification Pipeline)

```
Event Producer
     │
     │ POST /api/v1/events {event_type: "RISK-001", user_id, payload}
     ▼
┌────────────────────────────────────────────────────────────────────┐
│                      FastAPI Event Endpoint                        │
│  1. Validate event code against EVENT_REGISTRY                    │
│  2. Resolve channels (mandatory + user preference)                │
│  3. Generate notification_id (UUID)                               │
│  4. Publish to Kafka (notification.events topic)                  │
│  5. Publish to RabbitMQ (priority-based routing key)              │
│  6. Return 202 {notification_id, channels_targeted, eta_ms}       │
└────────────────────────────────────────────────────────────────────┘
     │ Kafka
     ▼
┌────────────────────────────────────────────────────────────────────┐
│                     Kafka Consumer Group                           │
│  1. Deserialize event payload                                     │
│  2. Check idempotency key (Redis SET NX)                         │
│  3. Call NotificationFactory.build_notifications()               │
└────────────────────────┬───────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│                    NotificationFactory                             │
│  For each eligible channel:                                       │
│  1. Check channel preference (UserPreference)                     │
│  2. Check event opt-out (disabled_event_types)                   │
│  3. Validate recipient (phone/email/FCM token)                   │
│  4. Render template (Jinja2, locale-aware)                       │
│  5. Create Notification row (status=PENDING)                     │
│  6. Log state: CREATED → state_log                               │
└────────────────────────┬───────────────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│                  NotificationDispatcher                            │
│  Compliance checks (in order):                                    │
│  1. Quiet hours check → SKIP (SkipReason.QUIET_HOURS)            │
│  2. DND check (SMS/WhatsApp only) → SKIP (DND_REGISTERED)        │
│     - Transactional events bypass DND                            │
│     - Cache result 24h in Redis                                  │
│  3. Frequency cap check:                                         │
│     - Global daily (12/day)                                      │
│     - Per-channel daily (SMS=5, Push=8, Email=3)                 │
│     - Per-category hourly (3/hour)                               │
│     - Same-type cooldown (15 min)                                │
│  All pass → update status=QUEUED → publish to Celery             │
└────────────────────────┬───────────────────────────────────────────┘
                         │ Celery queue="notifications"
                         ▼
┌────────────────────────────────────────────────────────────────────┐
│                   send_notification_task (Celery)                  │
│  1. Update status=PROCESSING                                      │
│  2. Check circuit breaker for provider                           │
│  3. Call channel.send() via ChannelRegistry                      │
│  4. On success: status=DELIVERED, log delivery_log entry         │
│  5. On failure:                                                   │
│     - retry_count < max_retries → status=RETRYING,              │
│       schedule retry_notification_task with exponential backoff  │
│     - retry_count >= max_retries → status=DEAD,                  │
│       insert into dead_letter_queue                              │
│  6. Log every transition to notification_state_log               │
└────────────────────────────────────────────────────────────────────┘
```

---

## Sequence Diagrams

### 1. Critical Notification Flow (RISK-001 Margin Call)

```
Margin     FastAPI    Kafka    KafkaConsumer  NotifFactory  Dispatcher  Celery   SMS
Engine      App       Topic                                              Worker   Provider
  │           │          │           │              │           │          │         │
  │─POST ────▶│          │           │              │           │          │         │
  │  /events  │          │           │              │           │          │         │
  │           │─publish─▶│           │              │           │          │         │
  │           │  202 ────│           │              │           │          │         │
  │◀──────────│          │◀─consume──│              │           │          │         │
  │           │          │           │─build()─────▶│           │          │         │
  │           │          │           │              │─DND bypass│          │         │  
  │           │          │           │              │  (TXNL)   │          │         │
  │           │          │           │              │─freq bypass          │         │
  │           │          │           │              │  (CRIT)   │          │         │
  │           │          │           │              │─render────│          │         │
  │           │          │           │              │  template │          │         │
  │           │          │           │              │─QUEUED────│          │         │
  │           │          │           │              │           │─apply────▶│         │
  │           │          │           │              │           │  async    │         │
  │           │          │           │              │           │          │─send()──▶│
  │           │          │           │              │           │          │ < 10s    │
  │           │          │           │              │           │          │◀─DLR─────│
  │           │          │           │              │           │          │ callback │
  │           │          │           │              │           │─DELIVERED─│         │
  │           │          │           │              │           │  state_log│         │
```

### 2. DND Check Flow (SMS Promotional)

```
Dispatcher      DND Service     Redis Cache    TRAI API
    │                │               │              │
    │─check_dnd()───▶│               │              │
    │                │─cache_get()──▶│              │
    │                │◀─miss─────────│              │
    │                │─query()───────────────────────▶│
    │                │◀─is_dnd=true──────────────────│
    │                │─cache_set(24h)▶│              │
    │                │                │              │
    │◀─DNDRegistered─│                │              │
    │  Error         │                │              │
    │─SKIP──────────▶DB               │              │
    │  (DND_REGISTERED)               │              │
    │─state_log()                     │              │
    │  actor=dnd_service              │              │
    │  metadata={dnd_check_ts, result}│              │
```

### 3. RabbitMQ Priority Queue Flow

```
NotifDispatcher   RabbitMQ Exchange   Priority Queues   Celery Worker
      │                  │                   │                │
      │─publish()────────▶│                   │                │
      │  routing_key=     │                   │                │
      │  "critical"       │─bind─────────────▶│CRITICAL(prio=9)│
      │                   │                   │                │
      │─publish()────────▶│                   │                │
      │  routing_key=     │─bind─────────────▶│HIGH(prio=7)    │
      │  "high"           │                   │                │
      │                   │                   │                │
      │                   │                   │─consume────────▶│
      │                   │                   │  CRITICAL first │
      │                   │                   │  then HIGH etc. │
```

### 4. Circuit Breaker Flow (Provider Outage)

```
CeleryWorker   CircuitBreaker   MSG91     Twilio   AlertService
     │               │            │          │           │
     │─is_open?──────▶│            │          │           │
     │◀─CLOSED────────│            │          │           │
     │─send()─────────────────────▶│          │           │
     │◀─503───────────────────────│           │           │
     │─record_failure()▶│          │          │           │
     │  (× 5 times)     │          │          │           │
     │                  │─OPEN─────│          │           │
     │                  │─alert────────────────────────────▶│
     │─is_open?──────────▶│         │          │           │
     │◀─OPEN              │         │          │           │
     │─failover──────────────────────────────▶│           │
     │  (Twilio)          │         │          │           │
     │◀─200───────────────────────────────────│           │
     │  (60s later)       │         │          │           │
     │                    │─HALF_OPEN          │           │
     │─test_send()──────────────────▶│         │           │
     │◀─200───────────────────────────│        │           │
     │                    │─CLOSED──  │        │           │
```

---

## Database ER Diagram

```
users (UUID PK)
  ├── user_preferences (1:1, FK user_id)
  ├── device_tokens (1:N, FK user_id)
  └── notifications (1:N, FK user_id)
        ├── notification_state_log (1:N, FK notification_id)
        │     [IMMUTABLE audit trail: CREATED→ENRICHED→ROUTED→QUEUED→SENT→DELIVERED→READ]
        ├── delivery_logs (1:N, FK notification_id)
        │     [Per-attempt delivery record with provider response]
        └── dead_letter_queue (1:1, FK notification_id)
              [Unresolved entries after max retries exhausted]

notification_templates (UUID PK)
  [UNIQUE: event_type + channel + locale]

notification_analytics (UUID PK)
  [UNIQUE: date + event_type + channel]
  [Daily aggregates for dashboard queries]

consent_records (UUID PK)
  [FK user_id → immutable opt-in/opt-out log with IP + consent_text]
```

---

## Technology Decision Records

| Decision | Choice | Rationale |
|---|---|---|
| Event Bus | Apache Kafka | 1M+ msg/sec, replay capability, audit log per spec A1.3 |
| Delivery Routing | RabbitMQ | Priority queues, DLQ, per spec B3.1 (60 scoring points) |
| Cache/State | Redis 7 | Atomic operations for frequency caps, DND cache, rate limiting |
| App Framework | FastAPI | Async native, auto OpenAPI docs, Pydantic validation |
| ORM | SQLAlchemy 2.0 async | Type-safe, async sessions, Alembic migrations |
| Template Engine | Jinja2 | StrictUndefined prevents missing-variable silences, HTML safe |
| SMS | Twilio (+ MSG91 fallback) | DLR callbacks, circuit breaker integration |
| Email | SendGrid | Delivery analytics, webhook events, sandbox mode |
| Push | Firebase FCM | Android + iOS + Web, multicast support |
| Task Queue | Celery + Redis | Retry scheduling, beat scheduler, Flower monitoring |
| Migrations | Alembic | Version-controlled schema, async engine support |
